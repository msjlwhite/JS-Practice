// J White
// Chapter 5 - Exercises
// Goal: Use the reduce method in combination with the concat method to “flatten” 
// an array of arrays into a single array that has all the elements of the original arrays.
/* let arrays = [[1, 2, 3], [4, 5], [6]];
// Your code here.
// → [1, 2, 3, 4, 5, 6] */

/// Ramone's Example
var arrays = [[1, 2, 3], [4, 5], [6]];
arrays.reduce(flat,current) => flat





// First try
// Variables
//var arrays = [[1, 2, 3], [4, 5], [6]];
//console.log (arrays);
//console.log(arrays.reduce());

/* function theFlattening()
{
    var newArray = reduce(arrays,1);
    return newArray;
    //console.log(reduce(arrays,1));
} */
/* // function
function flatArray ()
{
   //  for (var i = 0; i < arrays.length; i++)
   // {
        console.log(reduce(arrays));
  //  }
 
} //EOF
 */
// function call
//flatArray();

//theFlattening();

